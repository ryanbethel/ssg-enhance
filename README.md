# Simple Static Site Generator for Enhance

## Usage Example
Requires the manifest.mjs shown in the example folder at `examples/myproject/manifest.mjs`.
```sh
npm run build-static
```
Then run the static site:
```sh
npx start
```

